<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainDetailLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.lme.iudapp.view.fragments.UserDetailFragment">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/background_user_detail"
        android:alpha="0.2"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/activity_horizontal_margin">

        <EditText
            android:id="@+id/tvUserDetailName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:maxLength="15"
            android:inputType="text"
            android:enabled="false"
            android:textColor="@drawable/edt_user_detail"
            android:focusableInTouchMode="true"
            android:textAppearance="@style/Base.TextAppearance.AppCompat.Display3" />

        <EditText
            android:id="@+id/tvUserDetailBirthdate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:maxLength="10"
            android:inputType="date"
            android:enabled="false"
            android:focusable="false"
            android:textColor="@drawable/edt_user_detail"
            android:textAppearance="@style/Base.TextAppearance.AppCompat.Headline"
            android:padding="@dimen/activity_horizontal_margin"/>

    </LinearLayout>

</FrameLayout>
